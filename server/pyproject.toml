[project]
name = "server-mindtube"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pymongo",
    "pyjwt",
    "passlib[bcrypt]",
    "bcrypt==4.0.1",
    "pydantic",
    "pydantic-settings",
    "email-validator",
    "python-dotenv",
]
